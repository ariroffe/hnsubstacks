{
  "name": "worker",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "refresh": "curl https://worker.hnsubstacks.workers.dev/api/refresh",
    "test": "vitest",
    "backup_db": "npx wrangler d1 export hnsubstacks-db --remote -y --output=\"dbbackups/$(date +%Y_%m_%d)_backup.sql\""
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.12.4",
    "vitest": "~3.2.0",
    "wrangler": "^4.106.0"
  }
}